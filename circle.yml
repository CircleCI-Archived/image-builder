machine:
  environment:
    IMAGE_TAG: "trusty-$(date '+%y%m%d')-${CIRCLE_BUILD_NUM}-$(git rev-parse --short HEAD)"
    IMAGE_REPO: "circleci/build-image"

  pre:
    - echo 'no_cache() { git log --format=%B -n 1 | grep -q "no cache"; }' >> ~/.circlerc

  post:
    - sudo curl -L -o /usr/bin/docker 'https://s3.amazonaws.com/circle-downloads/docker-1.9.1-circleci'
    - sudo service docker start

dependencies:
  override:
    - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS

test:
  override:
    # Scratch is the latest cache
    - docker pull ${IMAGE_REPO}:scratch || true:
        timeout: 3600

    - ? |
        if $(no_cache); then
          docker build --no-cache -t ${IMAGE_REPO}:scratch .;
        else
          docker build -t ${IMAGE_REPO}:scratch .;
        fi
      :
        timeout: 3600

    - docker tag ${IMAGE_REPO}:scratch ${IMAGE_REPO}:${IMAGE_TAG}

    - docker push ${IMAGE_REPO}:scratch:
        timeout: 3600

    - docker push ${IMAGE_REPO}:${IMAGE_TAG}

    # Build a slightly modified image for unprivileged lxc
    - docker build --build-arg TARGET_UNPRIVILEGED_LXC=true -t ${IMAGE_REPO}:scratch-unprivileged .:
        timeout: 3600

    - docker tag ${IMAGE_REPO}:scratch-unprivileged ${IMAGE_REPO}:${IMAGE_TAG}-unprivileged

    - docker push ${IMAGE_REPO}:scratch-unprivileged:
        timeout: 3600

    - docker push ${IMAGE_REPO}:${IMAGE_TAG}-unprivileged:
        timeout: 3600

deployment:
  production:
    branch: master
    commands:
      - ./docker-export ${IMAGE_REPO}:${IMAGE_TAG}-unprivileged | aws s3 cp - s3://circle-downloads/$IMAGE_TAG.tar.gz --acl public-read:
          timeout: 3600
